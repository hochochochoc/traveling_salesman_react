// DEMOSPAGE
    // Menu
        // open secrets alert? Gmail
        // search function for maps
        // sort by continent 
        // issue where scrolling through maps doesn't work when loading that page but it does when coming from other page...
        // maps not loading properly
        // correct scrolling width adapted based on number of cards

        // disable auto-load of maps -- done
        // make menu a minimum of mobile friendly -- done
        // limit sliding to right -- done
        // borders -- done
        // slider for countries -- done
        // fix space between country cards -- done
        // add information to the cards, what idk like area, continent -- done
        // custom zoom, coordinates -- done
         // sidebar/menu from crewlineapp -- done

    // Landing
        // copy crewlineapp for landing page minimum

    // Map
        // if possible, prevent loading maps in sandbox automatically too while testing

    // Graph            
        // priorities:
            // actually apply react component logic
            // fix christofides bug when moving vertices
        
        // secondary edges, or actually show all the steps
        // clean up to use components, context etc.
        // fix stupid sidebar
        // fix loading problem with isMobile
        // make it look halfway decent in large screen too
        // adapt a bit to apps e.g. glovo with sliding menu option
        // add a selection menu at the bottom? like in hellofresh; Home, Algorithms, Validation      
        // fix christofides so it works perfectly
        // renderAlgorithm superfluous
        // put the paragraphs somewhere else
        // don't have the time appear for a milisecond when switching algorithms

        // change all the names, including prims and landingpage -- done
        // algorithm should work correctly, with a random vertex?? -- done
        // add visible lengths, display total cost/weight/length -- done
        // error where it deletes the graph when pressing start -- done
        // when changing validation selection, the graph and steps should reset -- done
        // replace the stupid button icons from w3 -- done
        // implement text mechanism -- done
        // make setMobile to make it responsive... buttons, vertices, numbers -- done
        // insert total length somewhere -- done
        // implement greedy and nearest neighbor algorithms -- done
        // change step 0 to start -- done
        // correct the links to validation and algorithms -- done
        // replace St with something better -- done
        // fix bug when saving -- done
        // responsify total edge weight in graph -- done
        // fix greedy algorithm -- done
        // fix texts not appearing for algorithms -- done
        // fix directions of text in demos -- done
        // add the algorithm for christofides -- done
        // write the content of the little texts -- done
        // show total time with last step -- done
        // fix 2-opt... -- done
        // make all algorithms work -- done
        // secondary edges for first nearest -- done
        // new paragraphs for twoOpt -- done
        // change the ui to something better -- done
        // maybe just try to adjust the chriftofides by secretly applying the twoOpt in the end -- done
        // should probably keep any changes made to the vertices when changing algorithms -- done



         // optional: show how vertices move from unconnected set to connected one
         // optional: make it so when I change from one algorithm to the next the changed vertices stay in the same location
         // optional: make it so numbers and edges aren't blocking each other (mobile too)
        // optional: look up import.meta.hot in graph and demoscontext again
        // optional: when moving the vertices: [Intervention] Ignored attempt to cancel a touchmove event with cancelable=false, for example because scrolling is in progress and cannot be interrupted.

// MISCELLANEOUS
    // add a cookies warning
    // add ads